{
  "name": "iivineri",
  "version": "3.1.5",
  "private": true,
  "license": "GPL-3.0-only",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "clean": "rimraf -g **/.turbo **/.next **/dist **/build **/node_modules && yarn cache clean"
  },
  "dependencies": {},
  "devDependencies": {
    "i18nexus-cli": "^3.5.0",
    "prettier": "^3",
    "rimraf": "^6",
    "sharp": "^0",
    "turbo": "^2.0.9"
  },
  "workspaces": [
    "applications/*"
  ],
  "engines": {
    "npm": ">=10.6.0",
    "node": ">=20.12.2"
  },
  "packageManager": "yarn@1.22.19"
}
